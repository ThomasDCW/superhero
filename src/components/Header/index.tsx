import ThemeButton from '../ThemeButton';
import SHeader from './style';

export default function Header() {
  return (
    <SHeader>
      <h1>SuperHero</h1>
      <ThemeButton />
    </SHeader>
  );
}
